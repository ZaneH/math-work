# Math Work

A collection of math worksheets and (eventually) other stuff.

## Worksheets

### Trig

| Link | Description |
|:-----|:------------|
| [↗](./trig/trig-and-antiderivatives-worksheet.pdf) | **Trig identities and antiderivatives** — derive everything from a small kernel of facts rather than memorizing forty formulas |

## Nix / Flake Users

Copy `.envrc.example` to `.envrc` and run `direnv allow`.

```sh
cp .envrc.example .envrc
direnv allow
```
